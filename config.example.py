# 数据库位置(相对路径)
db_path = "."

# 数据库名称
db_name = "database.db"

# 需要监听的文件夹根目录
# eg: root = "C:/"
root = "*Change me plz*"

# 主动检查更新的时间间隔(秒)
interval = 300

# 被动检查更新的冷却时间(秒)
cooldown = 60

# 检查更新成功后的延时(解决多文件连续更新)
delay = 5
